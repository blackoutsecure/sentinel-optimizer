{
  "name": "sentinel-optimizer",
  "version": "0.1.0",
  "private": true,
  "description": "Universal SIEM Collector — zero-trust, client-side ingestion and normalization for SIEM cost modeling and migration estimation.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
